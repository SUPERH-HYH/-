# 流体力学长管第二类问题求解器 - 用户文档

## 1. 程序概述

本程序是一个用于求解流体力学中**长管第二类问题**的图形界面应用程序。在流体力学中，长管第二类问题是指：**已知管道参数和总水头损失，求解管道中的流量和流态**。

程序采用Java语言开发，使用Swing构建图形界面，通过流量试算法迭代求解管道流量，并可自动判断流体的流动状态（层流或湍流）。

## 2. 系统要求

- 操作系统：Windows、macOS或Linux
- Java运行环境：JDK 8或更高版本
- 内存要求：至少256MB RAM
- 显示器分辨率：推荐1024x768或更高

## 3. 安装和运行

### 3.1 编译程序

如果您获得的是源代码，请按照以下步骤编译程序：

1. 确保您的系统已安装Java开发工具包（JDK）
2. 打开命令提示符（Windows）或终端（macOS/Linux）
3. 导航到程序目录：
   ```
   cd C:\Users\17720\Desktop\工作文档\个人事务\复习资料\复习软件\流体力学编程作业\java\simple
   ```
4. 执行编译命令：
   ```
   javac FluidFlowCalculator.java
   ```

### 3.2 运行程序

编译完成后，在同一目录下执行以下命令运行程序：

```
java FluidFlowCalculator
```

## 4. 使用说明

### 4.1 界面介绍

程序启动后，将显示如下界面：

- **输入参数区域**：位于界面上部，包含各种管道和流体参数的输入框
- **操作按钮区域**：位于界面中部，包含「计算流量」和「重置」两个按钮
- **结果显示区域**：位于界面下部，显示计算结果和说明

### 4.2 基本操作流程

1. **输入参数**：在输入区域填写或修改所需参数（程序已预设常用默认值）
2. **计算**：点击「计算流量」按钮执行计算
3. **查看结果**：在结果区域查看计算结果，包括流量、流速、雷诺数等
4. **重置**：如需重新计算，可点击「重置」按钮将所有参数恢复为默认值

## 5. 输入参数说明

| 参数名称 | 单位 | 说明 | 默认值 | 取值范围 |
|---------|------|------|-------|----------|
| 管道长度 | m | 管道的总长度 | 100.0 | > 0 |
| 管道直径 | m | 管道的内径 | 0.1 | > 0 |
| 绝对粗糙度 | m | 管道内壁的绝对粗糙度 | 0.00015 | ≥ 0 |
| 局部阻力系数 | - | 管道系统中所有局部阻力的总系数 | 0.0 | ≥ 0 |
| 流体密度 | kg/m³ | 流动介质的密度 | 998.2 | > 0 |
| 动力粘度 | Pa·s | 流动介质的动力粘度 | 1.003e-3 | > 0 |
| 总水头损失 | m | 流体在管道中流动时的总水头损失 | 10.0 | > 0 |

**注意**：所有输入参数必须为正数，程序会自动验证输入的有效性。粘度值支持科学计数法格式输入（如1.003e-3）。

## 6. 输出结果解释

计算完成后，程序将在结果区域显示以下内容：

- **流量**：管道中的体积流量，同时显示L/s和m³/s两种单位
- **流速**：流体在管道中的平均流速，单位为m/s
- **雷诺数**：判断流体流动状态的无量纲数
- **流态**：根据雷诺数判断的流动状态（层流或湍流）
- **沿程阻力系数**：管道壁面摩擦引起的阻力系数
- **沿程水头损失**：由沿程阻力引起的水头损失
- **局部水头损失**：由局部阻力（如弯头、阀门等）引起的水头损失
- **迭代次数**：计算过程中迭代的次数

此外，结果区域还会显示计算说明，包括使用的计算公式和方法。

## 7. 计算原理

程序采用**流量试算法**求解长管第二类问题，主要计算步骤如下：

1. **初始假设**：基于层流公式假设初始流量
2. **迭代计算**：
   - 根据当前流量计算雷诺数
   - 根据雷诺数计算沿程阻力系数
     - 层流状态（Re ≤ 2000）：使用泊肃叶公式 f = 64/Re
     - 湍流状态（Re > 2000）：使用科尔布鲁克公式迭代求解
   - 计算新的流量值
   - 检查收敛条件，若不满足则继续迭代
3. **结果判断**：判断流态并计算最终结果

### 7.1 关键公式

- **雷诺数计算**：
  
  Re = (ρ × v × D) / μ
  
  其中：ρ为流体密度，v为流速，D为管道直径，μ为流体动力粘度

- **流量计算**：
  
  Q = (π × D² × v) / 4

- **沿程阻力系数（层流）**：
  
  f = 64 / Re

- **科尔布鲁克公式（湍流）**：
  
  1/√f = -2 × log₁₀(ε/D/3.7 + 2.51/(Re×√f))
  
  其中：ε为绝对粗糙度

- **水头损失计算**：
  
  hf = f × (L/D) × (v²/2g) + ξ × (v²/2g)
  
  其中：L为管道长度，g为重力加速度，ξ为局部阻力系数

## 8. 常见问题解答

### 8.1 程序无法启动

**原因**：可能是Java环境未正确安装或配置。

**解决方案**：
- 检查是否已安装JDK 8或更高版本
- 验证环境变量JAVA_HOME是否正确设置
- 尝试重新安装Java运行环境

### 8.2 计算结果异常或不收敛

**原因**：可能是输入参数不合理或极端情况。

**解决方案**：
- 检查所有输入参数是否为正数
- 确认单位是否正确（如管道直径应以米为单位）
- 对于粘度值，可尝试使用科学计数法输入
- 检查绝对粗糙度值是否在合理范围内

### 8.3 界面显示异常

**原因**：可能是屏幕分辨率过低或Java Swing组件渲染问题。

**解决方案**：
- 调整显示器分辨率至1024x768或更高
- 尝试更新Java运行环境到最新版本

## 9. 示例计算

以下是一个典型的计算示例：

**输入参数**：
- 管道长度：100.0 m
- 管道直径：0.1 m
- 绝对粗糙度：0.00015 m
- 局部阻力系数：0.0
- 流体密度：998.2 kg/m³
- 动力粘度：1.003e-3 Pa·s
- 总水头损失：10.0 m

**预期结果**：
- 流量约为0.033 m³/s (33 L/s)
- 流速约为4.2 m/s
- 雷诺数约为418,000（湍流状态）
- 沿程阻力系数约为0.021

## 10. 版权信息

© 2023 流体力学编程作业项目组

本程序仅供学习和教学使用，未经授权不得用于商业用途。

---

## 更新日志

- v1.0：初始版本，实现基本的长管第二类问题求解功能